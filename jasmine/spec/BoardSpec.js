describe("Board", function() {
    var board, Board;

    beforeEach(function() {
        Board = window.minesweeper.Board
    });

    describe("creating a board", function () {
        xit("should return a board object when it is called with new", () => {
        })
    })
});
